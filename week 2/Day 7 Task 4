//Excel sheet column number

class Solution {
public:
    int titleToNumber(string s) {
        int l = s.length();
        int ans = 0;
        for(int i = 0 ;i < l ; i++ ){
            ans = ans*26 + (s[i] - 64);
        }
        return ans;
    }
};
